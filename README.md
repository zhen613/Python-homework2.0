# 從土地到餐桌：田頭水的食農教育 - Python 基礎教材

![食農教育海報](https://via.placeholder.com/800x400?text=從土地到餐桌) 

## 專案簡介
食農教育強調從農田生產（土地）到消費者食用（餐桌）的整個過程，特別在台灣的「田頭水」文化中，體現了農民與水資源的緊密連結。這包括了解農作物的種植、收成、運輸、加工與消費，目的是培養對食物來源的尊重與永續意識。

本專案使用 Python 作為工具，設計一份**基礎教材**，讓初學者透過簡單計算，探索食農相關概念。這份教材僅使用最基本的 Python 元素：`print`（輸出）、`input`（輸入）、變數（儲存資料）和運算子（如加減乘除），避免任何進階結構（如分支、迴圈、函式、類別），讓學習者輕鬆上手。

### 教材目標
- 了解食農過程中的基本數值計算，例如面積、產量、成本等。
- 練習使用變數儲存食農資料（如農田大小、作物重量）。
- 透過輸入與輸出，模擬食農情境的互動計算。
- 培養對食物生產的認識，例如水資源使用、作物轉換與市場價值。

### 基礎 Python 概念說明
1. **變數**：用來儲存數字或文字。例如，`area = 100` 表示將 100 儲存到變數 `area` 中。
2. **運算子**：用於計算，如 `+`（加）、`-`（減）、`*`（乘）、`/`（除）、`**`（次方）。例如，`total = 5 * 10` 計算 5 乘 10 並儲存到 `total`。
3. **print**：輸出結果到螢幕。例如，`print("Hello")` 會顯示 Hello。
4. **input**：從使用者輸入資料。例如，`weight = input("輸入重量：")` 會等待輸入並儲存到 `weight`。注意：input 預設儲存為字串，若需數字計算，需用 `int()` 或 `float()` 轉換，如 `weight = float(input("輸入重量："))`。

## 範例說明
教材範例皆以食農主題為主，每個範例包含程式碼、說明與執行步驟。學習者可直接在 Python 環境（如 IDLE 或線上編輯器）複製執行。

### 範例1：計算農田面積與作物產量（模擬土地種植階段）
這個範例計算農田的面積（長乘寬），並估計作物產量（面積乘單位產量），強調土地資源在食農過程中的基礎角色。

**執行步驟與食農連結**：
- 輸入長度如 10、寬度如 5、單位產量如 0.5，輸出面積 50 平方公尺、總產量 25 公斤。
- 這模擬田頭水的種植過程，提醒水資源（如灌溉）對產量的影響，教育永續使用土地。

程式碼見 
`Python`。

### 範例2：計算作物運輸成本（模擬從農田到市場階段）
這個範例計算作物重量與運輸距離的總成本（重量乘距離乘單位運費），連結食農中運輸的碳足跡與經濟考量。

**執行步驟與食農連結**：
- 輸入重量如 100、距離如 20，輸出總成本 1000 元。
- 這反映從土地到餐桌的運輸環節，教育減少距離（如在地消費）可降低成本與環境影響，體現田頭水的在地食農精神。

程式碼見 `examples/example2_transport_cost.py`。

### 範例3：計算食物單位轉換與營養價值（模擬餐桌消費階段）
這個範例將作物重量從公斤轉換為克，並計算簡單營養價值（重量乘單位熱量），強調食物從生產到食用的轉化。

**執行步驟與食農連結**：
- 輸入重量如 2、單位熱量如 1.5，輸出重量 2000 克、總熱量 3000 卡路里。
- 這連結餐桌階段，教育消費者了解食物來源與營養，促進對田頭水農作物的珍惜，避免浪費。

程式碼見 `examples/example3_nutrition.py`。

## 貢獻與聯絡
歡迎 fork 並貢獻更多範例！如有問題，請開 issue 或 email 到 your.email@example.com。

## 授權
MIT License - 自由使用與分享。

透過這些範例，學習者不僅掌握 Python 基礎，還能思考食農教育的永續性。建議練習修改變數值，探索不同情境！

---

*此專案由 Grok 協助生成，基於食農教育主題。*
